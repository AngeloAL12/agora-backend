meta {
  name: Update my Career
  type: http
  seq: 2
}

patch {
  url: {{base_url}}/users/me
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "career_id": 1
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
