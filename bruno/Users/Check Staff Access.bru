meta {
  name: Check Staff Access
  type: http
  seq: 4
}

get {
  url: {{base_url}}/users/staff
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
