meta {
  name: Create Complaint
  type: http
  seq: 1
}

post {
  url: {{base_url}}/complaints
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
