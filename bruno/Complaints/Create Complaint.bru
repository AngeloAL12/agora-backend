meta {
  name: Create Complaint
  type: http
  seq: 1
}

post {
  url: {{base_url}}/complaints
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "title": "...", 
    "description": "...", 
    "is_anonymous": false
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
