meta {
  name: Add Evidence
  type: http
  seq: 5
}

post {
  url: {{base_url}}/complaints/{{complaint_id}}/evidence
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:multipart-form {
  file: 
}

vars:pre-request {
  complaint_id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
