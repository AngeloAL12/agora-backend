meta {
  name: Get Building Detail
  type: http
  seq: 1
}

get {
  url: {{base_url}}/map/buildings/{{building_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

vars:pre-request {
  building_id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
