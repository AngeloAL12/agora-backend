meta {
  name: Join a club
  type: http
  seq: 7
}

post {
  url: {{base_url}}/clubs/{{club_id}}/members
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

vars:pre-request {
  club_id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
