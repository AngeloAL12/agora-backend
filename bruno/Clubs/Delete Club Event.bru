meta {
  name: Delete Club Event
  type: http
  seq: 14
}

delete {
  url: {{base_url}}/clubs/{{club_id}}/events/{{event_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

vars:pre-request {
  club_id: 1
  event_id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
