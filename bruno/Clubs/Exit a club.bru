meta {
  name: Exit a club
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/clubs/{{club_id}}/members/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

vars:pre-request {
  club_id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
