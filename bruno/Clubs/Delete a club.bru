meta {
  name: Delete a club
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/clubs/{{club_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

vars:pre-request {
  club_id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
