meta {
  name: Get Join Requests
  type: http
  seq: 18
}

get {
  url: {{base_url}}/clubs/{{club_id}}/join-requests
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

vars:pre-request {
  club_id: 2
}

settings {
  encodeUrl: true
  timeout: 0
}
