meta {
  name: Get Club Categories
  type: http
  seq: 10
}

get {
  url: {{base_url}}/clubs/categories
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
