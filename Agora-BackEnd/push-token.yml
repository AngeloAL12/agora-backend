info:
  name: push-token
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/push-token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
