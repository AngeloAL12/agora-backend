info:
  name: TestGoogleAuth
  type: http
  seq: 6

http:
  method: POST
  url: http://127.0.0.1:8000/auth/google/mobile-login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
