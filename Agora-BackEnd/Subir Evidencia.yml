info:
  name: Subir Evidencia
  type: http
  seq: 8

http:
  method: POST
  url: http://127.0.0.1:8000/complaints/1/evidence
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
